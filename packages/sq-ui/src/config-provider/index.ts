import { ConfigContext as _ConfigContext } from './context';
import _ConfigProvider from './ConfigProvider';

export type { ConfigProviderProps, ConfigSize } from './type';

export const ConfigContext = _ConfigContext;
export const ConfigProvider = _ConfigProvider;
